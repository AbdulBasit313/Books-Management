import { StyleSheet } from 'react-native'

export default ContentCardStyle = StyleSheet.create({
   card: {
      backgroundColor: 'white',
      borderRadius: 10,
      padding: 20,
      // shadowOpacity: 0.4,
      // shadowOffset: { width: 10, height: 10 },
      // shadowRadius: 2,
      elevation: 2,
      marginHorizontal: 20,
      marginVertical: 8,
      // shadowColor: 'blue',
      // borderLeftWidth: 0,
      // borderTopWidth: 0,
      // borderWidth: 2,
      // margin: 5,
      // borderRadius: 3,
   }
})